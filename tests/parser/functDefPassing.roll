/*
RUN: %parser "%s" > "%t"
RUN: %diff "%s.expect" "%t"
*/

// Declarations
Int h;
(Int, Int)->Int main;

// Statements
main = lambda (Int x, Int y)->Int {
    return x + 1;
};

h = 1;
putchar(main(1, 2));