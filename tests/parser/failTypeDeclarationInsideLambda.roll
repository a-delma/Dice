/*
Fail to parse a lambda that declares a type inside it.
XFAIL: *
RUN: %parser "%s" > "%t"
*/
[Int, Int]->Int sum;
sum = lambda (Int x, Int y)->Int {
    struct T {
        Int i;
    };
    return x + y;
};